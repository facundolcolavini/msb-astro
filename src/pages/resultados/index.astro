---
// index.astro
import ListHomes from "../../components/ListHomes.astro";
import Filtro from "../../components/preact/Filtro";
import Layout from "../../layouts/Layout.astro";
import type { APIResponseResultsRecords, File, Result } from "../../types/results.records.types";
import type { Results } from "../../types/selects.form.types";
const titlePage = "Resultados de búsqueda";
export const prerender = true;

let resultados: Result[] = []; // Estado para almacenar los resultados

---

<Layout title={titlePage}>
  <Filtro client:visible/>
</Layout>

