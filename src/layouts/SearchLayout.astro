---
import ListHomes from "../components/ListHomes.astro";
import FormFilters from "../components/preact/FormFilters";
import type { Results } from "../types/selects.form.types";
interface Props<T> {
  selects: Results;
  list: T;
}
const { selects, list } = Astro.props as Props<any>;
let notFound = false;
if (list.hasOwnProperty("error")) {
  notFound = true;
}else{
    notFound = false;
}
---

<div class="flex flex-col lg:flex-row">
  <aside class="w-full lg:w-1/4 lg:pr-4 sm:p-5 lg:p-0 ">
    <div class="bg-[#374151] p-4 rounded-lg">
      <h2 class="font-semibold text-white mb-4">FILTROS</h2>
      <FormFilters transition:persist transition:animate={"fade"}  selects={selects} client:load />
    </div>
  </aside>
  <main class="w-full lg:w-3/4">
    <!-- Reset filters -->

    {notFound ? (
      <div class="bg-[#374151] sm:p-2 rounded-lg">
        <h2 class="font-semibold mb-4 text-white sm:p-5">No se encontraron resultados...</h2>
      </div>
    ): <ListHomes listItems={list} />}
    
  </main>
</div>
